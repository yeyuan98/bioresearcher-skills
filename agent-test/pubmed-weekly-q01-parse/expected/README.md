# expected/ — FROZEN (2026-09-04)

`summary.json` is frozen ground truth, generated by:

```bash
cd <this case dir>
python3 groundtruth/generate.py   # uv run --with openpyxl python <skill parser> \
                                  #   fixtures/pubmed-sample.xml.gz -o <tmp>.xlsx \
                                  #   --summary-json expected/summary.json
```

Frozen values for the fixture: `article_count: 6`, `deleted_pmids:
["99999991", "99999992"]`. Regenerate (and re-tar `resources.tar.bz2`) only
when the fixture or the bioresearcher-pubmed-weekly parser changes.
